import React from "react";

const Navbar = () => {

    return (
        <nav className="navbar bg-primary-subtle mb-3">
            <div className="container-fluid d-flex justify-content-center align-items-center">
                <span className="px-2 py-1 border border-1 border-primary rounded-3 bg-white text-primary fw-bold fs-3 shadow-sm">
                    Semaforo
                </span>
            </div>
        </nav>
    );
};

export default Navbar